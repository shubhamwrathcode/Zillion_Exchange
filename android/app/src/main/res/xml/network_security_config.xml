<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Trust system + user CA certs for all connections (needed for older Android devices) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust preinstalled CAs -->
            <certificates src="system" />
            <!-- Trust user added CAs (useful for development/testing) -->
            <certificates src="user" />
        </trust-anchors>
    </base-config>

    <!-- Specific domain config for your backend -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">backend.zillion.wrathcode.com</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
