import React from "react";
import { View } from "react-native";
import { AppSafeAreaView, Toolbar } from "../../shared";

const p2pAds:React.FC=()=>{
    return(
    <AppSafeAreaView>
   <Toolbar isLogo={false} isSecond title="Profile"/>

    </AppSafeAreaView>
    )
}

export default p2pAds